# Backend
PORT=8080
JWT_SECRET=please_change_me
JWT_EXPIRES_IN=7d

# Docker (镜像/依赖源)
# 国内网络建议保持默认；如可直连 Docker Hub，可改为 BASE_IMAGE_REGISTRY=docker.io
BASE_IMAGE_REGISTRY=docker.m.daocloud.io
NPM_REGISTRY=https://registry.npmmirror.com

# Database (MySQL)
DB_DIALECT=mysql
DB_HOST=localhost
DB_PORT=3306
DB_NAME=pichub
DB_USER=pichub_user
DB_PASSWORD=pichub_pass

# External APIs
GEMINI_API_VERSION=v1
GEMINI_TEXT_MODEL=gemini-3-flash-preview
GEMINI_VISION_MODEL=gemini-3-flash-preview
GEMINI_API_KEY= # add your own API KEY（这里我暂时提供我自己的 API KEY）
AMAP_API_KEY=  # add your own API KEY
